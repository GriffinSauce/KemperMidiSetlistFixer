{
  "name": "convert-logic-midi",
  "version": "1.0.0",
  "description": "Converts copied midi data from logic to .mid file because logic's export sucks",
  "main": "fixOrder.js",
  "scripts": {
    "reload": "nodemon ./fixOrder.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "arraybuffer-to-buffer": "0.0.4",
    "fs-extra": "^3.0.1",
    "midievents": "^1.0.1",
    "midifile": "^1.0.4",
    "mkdirp": "^0.5.1"
  }
}
